ZeroQueue update - no OCR text anywhere in the UI + final README
=================================================================

What changed:
1. The small "Read N words via OCR." line after an image upload is removed
   from the customer chat. Both sides now show only the customer's question
   plus the image itself. OCR still runs internally for the AI's answers -
   it is just never displayed.
2. (Included from previous rounds) Refresh always starts a fresh AI chat;
   console shows question + image only; uploads are saved and served.
3. README.md: moss-style layout, now with the approved Problem Statement
   section (automate information, escalate exceptions).

Apply:
  cd /d E:\Hackathons\zeroqueue-hubble
  tar -xf "%USERPROFILE%\Downloads\zeroqueue-no-ocr-line.zip"

Then restart uvicorn and Ctrl+F5 both pages (chat and /agent).
Then: git add -A && git commit -m "remove OCR note line from chat; README problem statement" && git push

Tests: 64 passed.
